import { TooltipsItemDirective } from './tooltips-item.directive';

describe('TooltipsItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipsItemDirective();
    expect(directive).toBeTruthy();
  });
});
